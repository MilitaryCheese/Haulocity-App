import { createMuiTheme } from "@material-ui/core/styles";
const theme = createMuiTheme({
  palette: {
    primary: { main: "#49537b" },
    secondary: { main: "#fd746f" },
    primaryGradient: { main: "linear-gradient(to bottom, #49537b 0%,#5d5577 100%)" },
  },
});
export default theme;
